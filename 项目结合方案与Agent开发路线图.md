# 农业因果知识获取与解释推理 — 论文结合方案与Agent开发路线图

> 基于你的开题报告 + 两文件夹内论文，整合为可逐步实现的工程方案。

---

## 一、论文源码/思路分类（核心结论）

### ✅ 有源码可直接复用的论文（7篇）

| 论文 | 源码地址 | 可复用模块 |
|------|---------|-----------|
| **Event Causality Is Key** (NAACL 2024) | `https://github.com/insundaycathy/Event-Causality-Extraction` | LLM prompt因果事件关系抽取框架 |
| **KnowQA** (EMNLP 2024) | `https://github.com/du-nlp-lab/KnowQA` | 知识引导二元QA的文档级因果抽取 |
| **CausalRAG** (ACL 2025) | `https://github.com/Pwnb/CausalRAG` | 因果图增强RAG检索与生成 |
| **Paths to Causality** (KDD 2025) | GitHub（论文标注开源） | KG metapath子图 + Learning-to-Rank因果发现 |
| **Causal Agent** (Entropy 2024) | `https://github.com/Kairong-Han/Causal_Agent` | 因果Agent完整框架（工具+记忆+ReAct推理） |
| **CropGPT** (2024) | `https://github.com/Zaiwen/CropGPT` | 农业LLM + AgriKG知识图谱集成 |
| **ChatGPT4CausalReasoning** (EMNLP 2023) | `https://github.com/ArrogantL/ChatGPT4CausalReasoning` | LLM因果推理能力评估框架（用于测试你的系统） |

### ❌ 无源码仅作思路参考的论文（7篇，在你的两个文件夹中）

| 论文 | 可参考的核心思路 |
|------|-----------------|
| **LKCER** (COLING 2025) | 概念级异构事件图：同一事件概念的多个mention聚合为全局语义 |
| **SEAG** (ACL 2023) | 端到端text2text生成因果图 + 因果结构判别训练范式 |
| **GraphRAG-Causal** (2025) | Neo4j存储因果图 + 混合检索（语义embedding + 图结构） + XML prompt |
| **Document-level CRE** (EMNLP 2024) | 知识引导的二元问答做文档级因果抽取 |
| **Counterfactual Causal Inference** (2024) | 文本→因果图→反事实推理的端到端pipeline |
| **CFKGR** (EACL 2024) | 知识图谱上的反事实推理任务定义 |
| **Causal ML for Agroecosystems** (2024) | 因果ML在农业8个场景的应用综述 |

---

## 二、整体项目架构（研究内容1 + 研究内容2 一体化）

```
┌─────────────────────────────────────────────────────────────────────┐
│                    农业因果知识获取与推理系统                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌───────────────────────────────────────────────────────────┐     │
│  │  Layer 0: 农业领域知识底座                                 │     │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐ │     │
│  │  │ AgriKG       │  │ 农业术语词典  │  │ 农业本体约束     │ │     │
│  │  │ (CropGPT)    │  │ (自定义)     │  │ (自定义)         │ │     │
│  │  └──────────────┘  └──────────────┘  └──────────────────┘ │     │
│  └───────────────────────────────────────────────────────────┘     │
│                              │                                      │
│  ┌───────────────────────────▼───────────────────────────────┐     │
│  │  Layer 1: 研究内容1 — 因果知识自动获取                      │     │
│  │  ┌─────────────┐  ┌─────────────┐  ┌───────────────────┐ │     │
│  │  │ 1.1 农业因果 │  │ 1.2 因果    │  │ 1.3 因果链构建   │ │     │
│  │  │ 事件识别    │→│ 关系抽取    │→│ (多跳因果链)     │ │     │
│  │  └─────────────┘  └─────────────┘  └───────────────────┘ │     │
│  └───────────────────────────────────────────────────────────┘     │
│                              │                                      │
│  ┌───────────────────────────▼───────────────────────────────┐     │
│  │  Layer 2: 因果知识图谱存储层                                │     │
│  │  ┌──────────────────────────────────────────────────────┐ │     │
│  │  │  Neo4j 因果图: (事件节点) + (因果边) + (置信度属性)    │ │     │
│  │  │  存储: metapath子图 / 因果链 / 条件依赖               │ │     │
│  │  └──────────────────────────────────────────────────────┘ │     │
│  └───────────────────────────────────────────────────────────┘     │
│                              │                                      │
│  ┌───────────────────────────▼───────────────────────────────┐     │
│  │  Layer 3: 研究内容2 — 因果路径解释推理                      │     │
│  │  ┌─────────────┐  ┌─────────────┐  ┌───────────────────┐ │     │
│  │  │ 3.1 因果RAG │  │ 3.2 因果路径│  │ 3.3 可解释生成   │ │     │
│  │  │ 检索增强   │→│ 搜索与排序  │→│ (LLM机制解释)    │ │     │
│  │  └─────────────┘  └─────────────┘  └───────────────────┘ │     │
│  └───────────────────────────────────────────────────────────┘     │
│                              │                                      │
│  ┌───────────────────────────▼───────────────────────────────┐     │
│  │  Layer 4: 因果Agent（可选提高方向）                         │     │
│  │  ┌─────────────┐  ┌─────────────┐  ┌───────────────────┐ │     │
│  │  │ 因果任务    │  │ 工具调度    │  │ 反事实推理        │ │     │
│  │  │ 识别        │  │ (因果图API) │  │ (干预效果评估)    │ │     │
│  │  └─────────────┘  └─────────────┘  └───────────────────┘ │     │
│  └───────────────────────────────────────────────────────────┘     │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 三、研究内容1 详细结合方案：农业因果知识自动获取

> ### 精简方案说明（避免论文思路冗余融合）
>
> **诊断的问题**：原方案融合了 Event Causality Is Key + KnowQA + LKCER + SEAG + COMET + WordNet 等6+种方法，导致 Pipeline 存在 3 次 LLM 调用串联（1+N+M 次），多阶段判断不一致会降低 recall，且置信度评分 5 维加权稀释了 LLM 主导信号。
>
> **精简原则**：一次 LLM 调用解决一个子任务，规则做后处理。
>
> **精简后流程**：
> ```
> 农业文本
>   │
>   ├── 【LLM 调用 1 次】事件+因果对+因果链联合抽取（Event Causality Is Key 核心）
>   │     扩展 prompt 让 LLM 直接输出 causal_chains
>   │
>   ├── 【规则】概念聚合校准（同义词典，LKCER 思路简化，不依赖 WordNet）
>   │
>   ├── 【规则】本体方向过滤（agri_event_types.json 约束，过滤不合理因果方向）
>   │
>   ├── 【规则】置信度评分（3维：LLM置信度0.5 + 本体一致0.3 + 证据匹配0.2）
>   │
>   └── 【规则】因果链构建（图算法 DFS + 优先采用 LLM 输出链，无 LLM 判别）
> ```
>
> **移除的冗余模块**：
> - ❌ 模块1.2 的 LLM 二元QA判断（KnowQA 简化版）→ 改为纯规则本体过滤
> - ❌ 模块1.3 的 LLM 链判别（SEAG 判别思想）→ 改为纯图算法 + LLM链优先
> - ❌ COMET 知识增强（农业领域噪声大）→ 由同义词典替代
> - ❌ WordNet 概念聚合（中文农业覆盖差）→ 由自定义同义词典替代
> - ❌ 置信度评分的 relation_type/concept_known 维度（与其他模块重复）
>
> **保留的核心创新**：
> - ✅ 农业事件类型本体约束（agri_event_types.json）
> - ✅ 农业同义词典概念聚合（agri_synonym_dict.json）
> - ✅ 本体方向因果过滤规则
> - ✅ 图算法因果链合并（DFS + 几何均值置信度）
>
> **效果**：LLM 调用从 `1+N+M` 降为 `1` 次，避免多阶段判断不一致导致 recall 下降。

### 3.1 模块 1.1：农业因果事件识别（事件+因果对+因果链联合抽取）

**复用的论文与方法：**

| 来源 | 用什么 | 怎么用 |
|------|--------|--------|
| **Event Causality Is Key** ✅ | LLM prompt框架（Figure 1的prompt模板） | 将通用事件列表prompt改为农业事件列表prompt，加入农业领域few-shot示例 |
| **LKCER** ❌思路 | 概念级事件异构图的构建方法 | 用WordNet聚合农业同义事件mention（如"落花"/"花脱落"/"花凋谢"），构建事件概念节点 |
| **CropGPT** ✅ | AgriKG中的农业实体/事件类型 | 作为农业事件类型定义的基础，提供农业事件的本体分类 |

**具体实现方案：**

```
输入: 农业文本（论文摘要/技术手册段落）
  │
  ├── Step 1: 事件mention识别
  │   方法: 基于RoBERTa + 农业领域微调（参考LKCER的编码方式）
  │   或者直接用LLM few-shot prompt（参考Event Causality Is Key）
  │
  ├── Step 2: 事件概念聚合
  │   方法: WordNet + 自定义农业同义词表 → 构建概念级事件图
  │   示例: "落花" / "花脱落" / "花凋谢" → 统一概念"FLOWER_DROP"
  │
  └── Step 3: 事件类型分类
      方法: 映射到农业因果事件类型体系
      类型: CAUSE_EVENT（原因事件）/ EFFECT_EVENT（结果事件）/ INTERMEDIATE（中间事件）
```

**Agent开发任务：**
1. 从 Event Causality Is Key 的 GitHub 拉取代码，理解其prompt结构
2. 设计农业领域的few-shot示例（高温→落花、干旱→减产等）
3. 构建农业事件概念聚合词典
4. 实现事件mention检测 + 概念聚合pipeline

---

### 3.2 模块 1.2：因果关系抽取

**复用的论文与方法：**

| 来源 | 用什么 | 怎么用 |
|------|--------|--------|
| **KnowQA** ✅ | 知识引导二元QA框架 + 事件结构构建 | 将因果抽取转化为"事件A是否导致事件B？"的问答，用农业领域知识做引导 |
| **LKCER** ❌思路 | LLM知识增强 + COMET知识过滤 | 用COMET生成候选因果知识，LLM过滤+丰富，再与事件对匹配 |
| **ChatGPT4CausalReasoning** ✅ | 因果幻觉检测框架 | 用于验证你系统输出的因果关系是否可靠，检测因果幻觉 |

**具体实现方案：**

```
输入: 已识别的事件对 (E_cause, E_effect)
  │
  ├── Step 1: 知识增强
  │   ├── 方式A: COMET生成候选因果知识 → LLM过滤（LKCER思路）
  │   └── 方式B: AgriKG查询已知因果关系 → 作为领域约束
  │
  ├── Step 2: 二元QA判断
  │   方法: KnowQA框架
  │   问题模板: "在农业语境下，事件A是否导致事件B？"
  │   知识引导: 将AgriKG中的相关三元组注入prompt
  │
  └── Step 3: 可信度评估
      输出: 因果关系 + 置信度分数 + 证据来源
      验证: 用ChatGPT4CausalReasoning的评估框架测因果幻觉
```

**Agent开发任务：**
1. 从 KnowQA GitHub 拉取代码，理解二元QA的事件结构构建流程
2. 适配为农业领域的QA模板
3. 集成 COMET + LLM 知识增强管道（LKCER思路）
4. 实现置信度评分机制

---

### 3.3 模块 1.3：因果链构建

**复用的论文与方法：**

| 来源 | 用什么 | 怎么用 |
|------|--------|--------|
| **SEAG** ❌思路 | text2text因果图生成 + 因果结构判别训练 | 将因果链生成建模为序列生成任务，训练模型区分正确/错误因果链 |
| **Event Causality Is Key** ✅ | 多跳因果关系的图结构输出 | 在其prompt基础上扩展为多跳因果链输出 |
| **Paths to Causality** ✅ | metapath子图概念 | 用于表示和验证多跳因果链的结构正确性 |

**具体实现方案：**

```
输入: 已抽取的单因果对集合
  │
  ├── Step 1: 因果链候选发现
  │   方法: 基于事件共现 + 时间顺序 + 领域知识约束
  │   示例: 高温→花粉活性下降 + 花粉活性下降→授粉失败 → 合并为因果链
  │
  ├── Step 2: 因果链验证
  │   方法: LLM判别 + 因果结构判别训练（SEAG思路）
  │   判别标准: 时序合理性 / 机制合理性 / 中间节点必要性
  │
  └── Step 3: 因果链结构化输出
      格式: {cause_chain: ["高温胁迫", "花粉活性下降", "授粉失败", "落花"],
              confidence: 0.87,
              evidence: ["论文A第3段", "技术手册B"]}
```

**Agent开发任务：**
1. 实现因果链合并算法（基于事件重叠检测）
2. 设计因果链判别prompt（正例/负例对比训练）
3. 实现因果链JSON结构化输出

---

## 四、研究内容2 详细结合方案：因果路径解释推理

### 4.1 因果知识图谱存储与检索（Layer 2 桥接层）

**复用的论文与方法：**

| 来源 | 用什么 | 怎么用 |
|------|--------|--------|
| **GraphRAG-Causal** ❌思路 | Neo4j + 混合检索架构 | Neo4j存因果图，语义embedding + 图结构混合检索 |
| **CausalRAG** ✅ | 因果图增强RAG检索 | 用因果边保持上下文连续性，替代纯语义相似度检索 |
| **Paths to Causality** ✅ | metapath子图排序 | 用Learning-to-Rank选择最有信息量的因果子图 |

**具体实现方案：**

```
Neo4j 图Schema设计:
  ┌──────────────────────────────────────────────────────┐
  │  Node: Event                                         │
  │    - event_id: str                                   │
  │    - event_text: str       (如"高温胁迫")             │
  │    - event_type: enum      (CAUSE/EFFECT/INTERMEDIATE)│
  │    - concept: str          (概念聚合后的统一标签)      │
  │    - embedding: vector     (语义向量)                 │
  │    - source: str           (来源论文/段落)            │
  │                                                      │
  │  Edge: CAUSES                                        │
  │    - confidence: float                                │
  │    - relation_type: enum  (DIRECT/INDIRECT/CONDITIONAL)│
  │    - evidence: str                                    │
  │    - temporal_order: int  (在因果链中的位置)           │
  └──────────────────────────────────────────────────────┘
```

**Agent开发任务：**
1. 搭建Neo4j本地实例（Docker或本地安装）
2. 定义图Schema并创建索引
3. 实现研究内容1产出的因果知识写入Neo4j
4. 实现 CausalRAG 的因果图检索逻辑
5. 实现 metapath 子图提取与排序（Paths to Causality思路）

---

### 4.2 模块 3.1 + 3.2：因果路径搜索与排序

**复用的论文与方法：**

| 来源 | 用什么 | 怎么用 |
|------|--------|--------|
| **CausalRAG** ✅ | 因果上下文保持 + 检索增强 | 用户问"为什么落花？"→ 在因果图中找到从原因到结果的最优路径 |
| **Paths to Causality** ✅ | metapath子图 + Learning-to-Rank | 对多条候选因果路径进行排序，选出最合理/最完整的 |
| **GraphRAG-Causal** ❌思路 | 混合Cypher查询 | 结合语义相似度和图结构得分检索最相关因果路径 |

**具体实现方案：**

```
输入: 用户问题 "为什么番茄落花？"
  │
  ├── Step 1: 问题解析
  │   提取: 目标事件="落花", 领域="番茄", 意图="因果解释"
  │
  ├── Step 2: 因果路径检索（CausalRAG方式）
  │   在Neo4j中执行:
  │   MATCH path = (start:Event)-[:CAUSES*1..5]->(end:Event)
  │   WHERE end.event_text CONTAINS "落花"
  │   RETURN path
  │
  ├── Step 3: 路径排序（Paths to Causality的LTR方法）
  │   特征: 路径长度 / 平均置信度 / 节点覆盖度 / 语义连贯性
  │   排序: 点对点/列表级Learning-to-Rank
  │
  └── Step 4: Top-K路径输出
      输出: 排序后的因果路径列表
```

**Agent开发任务：**
1. 实现Cypher路径搜索查询
2. 实现路径排序特征提取
3. 训练或配置Learning-to-Rank模型
4. 实现混合检索得分计算

---

### 4.3 模块 3.3：可解释结果生成

**复用的论文与方法：**

| 来源 | 用什么 | 怎么用 |
|------|--------|--------|
| **CausalRAG** ✅ | 因果增强的LLM生成 | 将检索到的因果路径作为context注入LLM prompt |
| **Causal Agent** ✅ | Agent推理链 + 解释生成模块 | ReAct风格的多步推理 + 最终解释生成 |

**具体实现方案：**

```
Prompt模板:
┌─────────────────────────────────────────────────────────┐
│ 你是一个农业因果推理专家。请基于以下因果路径，生成     │
│ 自然语言的机制解释。                                     │
│                                                         │
│ 用户问题: {user_query}                                   │
│                                                         │
│ 因果路径:                                                │
│ {retrieved_causal_path}                                  │
│                                                         │
│ 要求:                                                    │
│ 1. 按因果链条逐步解释，说明每一步的机制                   │
│ 2. 使用农业专业知识，但语言通俗易懂                       │
│ 3. 标注证据来源                                          │
│                                                         │
│ 解释:                                                    │
└─────────────────────────────────────────────────────────┘

输出示例:
"持续高温胁迫首先导致花粉母细胞活性下降，这是因为温度超过35℃时
花粉萌发率显著降低（来源：XXX论文）。花粉活性下降进一步导致授粉
效率降低，柱头可授性下降（来源：YYY技术手册）。最终，授粉失败
直接导致落花率增加。"
```

**Agent开发任务：**
1. 设计因果解释prompt模板
2. 实现因果路径到自然语言的转换
3. 添加证据溯源功能
4. 对比有无因果图的解释质量（消融实验）

---

## 五、Agent逐步开发路线图（共7个Phase）

### Phase 1: 环境搭建与源码拉取（预计1-2天）

```
□ 1.1 克隆所有开源仓库到本地
    git clone https://github.com/insundaycathy/Event-Causality-Extraction.git
    git clone https://github.com/du-nlp-lab/KnowQA.git
    git clone https://github.com/Pwnb/CausalRAG.git
    git clone https://github.com/Kairong-Han/Causal_Agent.git
    git clone https://github.com/Zaiwen/CropGPT.git
    git clone https://github.com/ArrogantL/ChatGPT4CausalReasoning.git
    git clone [Paths to Causality的GitHub地址]

□ 1.2 搭建Python虚拟环境 + 安装依赖
    conda create -n agri-causal python=3.10
    pip install torch transformers neo4j openai langchain ...

□ 1.3 搭建Neo4j数据库（Docker）
    docker run -p 7474:7474 -p 7687:7687 neo4j:5

□ 1.4 准备农业测试数据
    - 从知网/万方爬取农业论文摘要（50-100篇）
    - 手工标注30-50个因果对作为测试集
```

### Phase 2: 农业因果事件识别（基于 Event Causality Is Key + LKCER思路）

```
□ 2.1 理解 Event Causality Is Key 的prompt框架
    - 阅读其Figure 1的prompt模板
    - 跑通示例代码

□ 2.2 设计农业领域因果事件识别prompt
    - 编写农业few-shot示例（5-10个农业因果场景）
    - 加入农业事件类型约束

□ 2.3 构建农业事件概念聚合模块（LKCER思路）
    - 收集农业同义事件词表
    - 实现WordNet + 自定义词典的概念聚合

□ 2.4 测试与迭代
    - 在手工标注的农业测试集上评估
    - 计算Precision/Recall/F1
```

### Phase 3: 因果关系抽取（基于 KnowQA + LKCER思路）

```
□ 3.1 理解 KnowQA 的事件结构构建 + 二元QA框架
    - 跑通示例代码
    - 理解 Event Detection → Argument Extraction → Relation Extraction 流程

□ 3.2 适配为农业因果QA
    - 设计农业因果QA问题模板
    - 将AgriKG知识注入QA prompt

□ 3.3 实现知识增强管道（LKCER思路）
    - 接入COMET生成候选因果知识
    - 实现LLM知识过滤与丰富

□ 3.4 实现可信度评估
    - 设计置信度评分规则
    - 用ChatGPT4CausalReasoning框架做因果幻觉检测
```

### Phase 4: 因果链构建（基于 SEAG思路 + Event Causality Is Key）

```
□ 4.1 实现因果链合并算法
    - 基于事件重叠的链合并
    - 基于时间顺序的链排序

□ 4.2 设计因果链判别prompt
    - 正例: 正确的农业因果链
    - 负例: 打乱顺序/缺失中间节点/引入无关事件

□ 4.3 实现因果链结构化输出
    - JSON格式输出
    - 包含证据溯源
```

### Phase 5: 因果知识图谱存储与检索（基于 GraphRAG-Causal思路 + CausalRAG + Paths to Causality）

```
□ 5.1 设计并创建Neo4j图Schema
    - Event节点 + CAUSES边
    - 创建向量索引

□ 5.2 实现因果知识写入Neo4j
    - 将Phase 2-4的产出写入Neo4j
    - 生成embedding并存储

□ 5.3 实现 CausalRAG 检索逻辑
    - 因果路径Cypher查询
    - 语义embedding + 图结构混合检索

□ 5.4 实现 metapath 子图排序（Paths to Causality思路）
    - 提取候选metapath子图
    - 实现Learning-to-Rank排序
```

### Phase 6: 因果路径推理与解释生成（基于 CausalRAG + Causal Agent）

```
□ 6.1 实现问题解析模块
    - 识别因果问题类型
    - 提取目标事件和领域

□ 6.2 实现因果路径搜索与排序
    - Cypher路径搜索
    - Top-K路径排序

□ 6.3 实现可解释生成
    - 因果路径 → 自然语言解释prompt
    - 证据溯源标注

□ 6.4 构建端到端推理pipeline
    - 问题输入 → 路径检索 → 解释生成
    - 端到端测试与评估
```

### Phase 7: 评估与论文实验（基于 ChatGPT4CausalReasoning + 自建测试集）

```
□ 7.1 构建农业因果推理测试集
    - 50-100个因果问答对
    - 包含标准因果路径答案

□ 7.2 设计对比实验
    - Baseline: 纯LLM（无因果图）
    - Ours: LLM + 因果图增强
    - 评估指标: 解释准确性 / 完整性 / 可读性

□ 7.3 因果幻觉分析
    - 用ChatGPT4CausalReasoning框架评估
    - 量化因果幻觉率

□ 7.4 撰写论文实验部分
    - 数据 + 图表 + 分析
```

---

## 六、关键技术决策建议

### 6.1 LLM选择
- **推荐**: 使用API形式的大模型（GPT-4o / Claude / 豆包），降低本地部署成本
- 农业领域微调: 如果资源允许，用CropGPT作为基座模型进行农业因果微调

### 6.2 图数据库选择
- **推荐**: Neo4j（GraphRAG-Causal已验证 + 生态成熟）
- 备选: NetworkX（轻量原型验证阶段）

### 6.3 农业领域数据
- 优先级1: 自建小规模标注集（30-50因果对）用于快速验证
- 优先级2: 从知网/万方爬取农业论文摘要构建无标注语料
- 优先级3: 利用CropGPT的AgriKG作为领域知识约束

### 6.4 创新点定位
1. **场景创新**: 首次将LLM因果抽取系统性地应用于农业领域
2. **方法创新**: 农业领域知识约束的因果抽取（AgriKG + 自定义农业本体）
3. **工程创新**: 端到端的"抽取→存储→推理→解释"完整pipeline

---

## 七、风险提示与应对

| 风险 | 等级 | 应对方案 |
|------|------|---------|
| 农业因果标注数据稀缺 | 高 | Phase 1先建小规模标注集 + 零样本/少样本方案 |
| LLM因果幻觉 | 高 | 用ChatGPT4CausalReasoning框架持续检测 + 领域知识约束 |
| 开源代码在农业场景效果差 | 中 | 先快速验证基线，若不理想则调整prompt策略 |
| Neo4j复杂查询性能 | 低 | 先小规模图验证，必要时加索引优化 |
| 创新点不够突出 | 中 | 聚焦农业因果的特殊性（时序性/条件性/多因素耦合） |

---

> **本文档可直接作为Agent的task list执行。每个Phase下的□条目即是一个可执行的子任务。**
