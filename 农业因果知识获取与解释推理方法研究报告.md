# 基于大语言模型的农业因果知识获取与解释推理方法研究

## 摘要

农业生产的复杂性与气候变化加剧了作物胁迫事件频发，精准识别"环境因子→功能症状→产量品质"的因果链条对智能决策具有重要意义。然而，传统农业知识图谱以静态实体-关系为主，缺乏细粒度因果建模能力；主流因果抽取方法依赖多阶段串联 LLM 调用，存在成本高、一致性差、Recall 坍塌等问题；因果问答环节又普遍采用黑盒 RAG，缺乏可解释性。

本文围绕"农业因果知识自动获取"与"面向认知任务的因果路径解释推理"两个研究内容展开。研究内容1提出"1 次 LLM 联合抽取 + 规则后处理 + 本体对齐"的端到端因果事件抽取流水线，并重点解决了从项目组农业知识图谱本体 AKO-F v1.9（61 个 OWL 类）到个人因果图的迁移对齐问题：通过实体层 4 大类→14 子类→概念三层映射、3 种因果子关系（TRIGGERS / PROPAGATES / ALLEVIATES）继承父关系 causes、多 label 节点存储（:Entity + :class_id + :sub_class_id）等机制，保证抽取内容可直接落库到项目组本体的 Neo4j 实例。研究内容2构建 CausalRAG（因果图增强检索增强生成）推理框架，将因果路径检索与 LLM 生成深度耦合，提供可追溯的解释链。基于真实农业文本构建的因果图实测规模：119 节点、239 条因果边（TRIGGERS 111、PROPAGATES 125、ALLEVIATES 3），另含 71 条禁止因果边（FORBIDDEN_CAUSES）用于主动学习。

与 2024-2025 年主流论文（CausalRAG @Findings of ACL 2025、Paths to Causality @KDD 2025、LKCER @COLING 2025、Event Causality Is Key @NAACL 2024 等）对比表明，本文方法在"LLM 调用次数最小化""领域本体深度对齐""中文农业领域适用性""多子关系因果建模"四个维度具备竞争力，方法路径未过时。

**关键词：** 大语言模型；农业知识图谱；因果事件抽取；本体对齐；CausalRAG；可解释推理

---

## 1 引言

### 1.1 研究背景

农业知识图谱（Agricultural Knowledge Graph, AKG）是农业智能化的核心基础设施之一。当前主流农业知识图谱（如 AgriKG、ShizishanGPT 内置 AgriKG）多以"实体-属性-关系"形式组织静态知识，能够支撑简单的实体查询、问答和推荐任务，但对"为什么发生""如何传导""如何缓解"这类涉及因果链条的认知任务支撑有限。

然而，农业生产本质上是一个高度动态的因果系统：环境因子（高温、干旱、盐碱）触发功能症状（光合作用下降、养分吸收失衡），进而传导到产量下降、品质劣化；防治措施（灌溉、施肥、抗逆剂）则缓解上述症状。准确建模这一因果链条，是从"静态知识查询"迈向"动态因果推理"的关键。

### 1.2 问题陈述

本文要解决的核心问题包括：

1. **因果事件抽取成本高、一致性差**：主流方法采用"事件识别→因果对抽取→因果链拼接"多阶段串联 LLM 调用，单条文本需调用 1+N+M 次 LLM，成本高且多阶段输出不一致，导致 Recall 坍塌。
2. **领域本体未对齐**：抽取的因果对往往停留在自由文本概念层，无法直接映射到领域本体，难以进入项目组统一知识图谱体系。
3. **因果问答黑盒化**：现有 RAG 用 embedding 检索片段后直接生成答案，缺乏因果路径的可追溯解释，难以满足农业专家"看到推理过程"的认知需求。
4. **概念级事件聚合缺失**：同一事件在不同文本中表述多样（如"高温""热害""持续35℃以上高温"），不聚合将导致同义节点在图中爆炸式增殖。

### 1.3 研究意义

- **学术意义**：探索"LLM 抽取 + 规则对齐 + 本体存储"的轻量化因果建模路径，为垂直领域因果知识图谱构建提供可复用范式。
- **工程意义**：抽取内容直接进入项目组 AKO-F v1.9 本体 Neo4j 实例，与项目本体无缝衔接，避免知识孤岛。
- **应用意义**：CausalRAG 推理框架可为农业专家、农技推广人员提供"问题→因果路径→解释文本"的可解释决策支持。

---

## 2 研究内容1：基于大语言模型的农业因果知识自动获取方法

### 2.1 研究目标

构建端到端的农业因果事件抽取与知识图谱构建流水线，实现：
- **抽取目标**：从非结构化农业文本中识别事件 mention、因果对、因果链；
- **对齐目标**：将抽取的概念映射到项目组 AKO-F v1.9 本体；
- **存储目标**：以多 label 节点和 3 种因果子关系边存储到 Neo4j；
- **成本目标**：单条文本仅调用 1 次 LLM，其余环节纯规则。

### 2.2 要解决的问题

1. LLM 多阶段串联导致的成本与一致性坍塌；
2. 自由文本 mention 到本体标准概念的多对多归一化；
3. 本体方向校验（避免"Treatment→产量下降"这类语义反向错误）；
4. 极性检查（避免"防效"被误判为病害加重等反向极性错误）；
5. 概念自环（同一概念因果到自身）的禁止；
6. 抽取内容如何无缝落到项目组本体存储。

### 2.3 方法

#### 2.3.1 整体流水线

抽取流水线位于 [bate/src/pipeline.py](file:///c:\Users\lenovo\Desktop\开题\bate\src\pipeline.py)，五步串联：

1. **LLM 联合抽取**（[event_identifier.py](file:///c:\Users\lenovo\Desktop\开题\bate\src\event_extraction\event_identifier.py)）：1 次 LLM 调用同时输出事件 mention、因果对、因果链候选，避免多阶段串联。
2. **规则概念聚合**（[concept_aggregator.py](file:///c:\Users\lenovo\Desktop\开题\bate\src\event_extraction\concept_aggregator.py)）：6 级策略将 mention 归一化到标准概念并映射到子类：
   - 策略1：mention 精确命中 mention_to_concept；
   - 策略2：LLM concept 在本体标准概念集合；
   - 策略3：LLM concept 在 concept_to_subtype 显式映射；
   - 策略4：mention 模糊包含匹配；
   - 策略5：启发式关键词正则（如"产量下降|减产"→"产量下降"）；
   - 策略6：LLM concept 兜底 + 启发式子类推断。
3. **因果关系确认 + 方向校验 + 极性检查**（[causal_extractor.py](file:///c:\Users\lenovo\Desktop\开题\bate\src\relation_extraction\causal_extractor.py)）：基于本体方向规则（EnvFactor→FunctionalSymptom 才允许 TRIGGERS；Treatment 必须 inhibit 才允许 ALLEVIATES）和极性规则（Treatment 后若 effect 为"病害加重/产量下降"等负向概念，要求文本显式声明措施失败，否则过滤）。
4. **置信度评分**（[confidence_scorer.py](file:///c:\Users\lenovo\Desktop\开题\bate\src\relation_extraction\confidence_scorer.py)）：三维加权 LLM(0.5) + ontology(0.3) + evidence(0.2)，其中 evidence 维含支持证据数加分（0.05/n，上限 0.1），conf < 0.3 的低置信因果对被过滤。
5. **因果链构建**（[chain_builder.py](file:///c:\Users\lenovo\Desktop\开题\bate\src\relation_extraction\chain_builder.py)）：基于图算法 DFS + LLM 链优先级构建多跳因果链。

#### 2.3.2 从项目组本体 AKO-F v1.9 迁移对齐到个人因果图

**这一节是研究内容1的核心创新点**，详细说明抽取出的因果知识如何对齐并存储到项目组本体中。

##### 2.3.2.1 项目组本体结构

项目组本体文件 [agri-kg-ontology.ttl](file:///c:\Users\lenovo\Desktop\开题\agri-kg-ontology.ttl)（AKO-F v1.9）共定义 61 个 OWL 类：
- 52 个实体类
- 9 个事件类
- 14 个关系类

本体采用"实体大类 → 子类 → 概念"三层分类设计，事件层（EventType）不作为图谱节点，仅承载动态知识。

##### 2.3.2.2 实体层 4 大类迁移对齐

抽取的实体节点全部对齐到本体实体层 4 大类，配置文件为 [bate/data/agri_event_types.json](file:///c:\Users\lenovo\Desktop\开题\bate\data\agri_event_types.json)：

| 实体大类 (class_id) | 中文名 | 子类数 | 子类 ID（节选） |
| --- | --- | --- | --- |
| EnvFactor | 环境因子 | 14 | WeatherParam / SoilParam / WaterQualityParam 等 |
| BioticStress | 生物胁迫 | 4 | Disease / Pest / Pathogen / WeedCompetition |
| Treatment | 防治措施 | 1 | ManagementMeasure |
| FunctionalSymptom | 功能症状 | 5 | PhysiologicalSymptom / ReproductiveSymptom / YieldSymptom / QualitySymptom / StressResponseSymptom |

环境因子大类按项目组本体进一步细化为 3 个父类 + 14 个细粒度子类：

- **天气 (WeatherParam)** 7 子类：AirTemperatureParam（空气温度）/ AirHumidityParam（空气湿度）/ PrecipitationParam（降水）/ LightParam（光照）/ WindSpeedParam（风速）/ CO2Param（CO₂浓度）/ AirPressureParam（气压）
- **土壤 (SoilParam)** 4 子类：SoilTemperatureParam / SoilMoistureParam / SoilNutrientParam / SoilPHParam
- **水质 (WaterQualityParam)** 3 子类：WaterPHParam / WaterSalinityParam / WaterHeavyMetalParam

这一映射在 [concept_aggregator.py](file:///c:\Users\lenovo\Desktop\开题\bate\src\event_extraction\concept_aggregator.py) 的 `concept_index` 中自动生成：本体 concepts 嵌套结构 → 概念到 "class_id.sub_class_id" 的索引（如 "高温胁迫" → "EnvFactor.WeatherParam"），无需硬编码概念列表，新增实体时自动纳入 LLM prompt 选项。

##### 2.3.2.3 因果关系 3 子关系对齐

项目组本体父关系 `causes` 在本研究中细化为 3 种因果子关系，对应不同起点-终点实体类组合：

| 子关系 | 中文语义 | 起点实体类 → 终点实体类 | 应用场景 |
| --- | --- | --- | --- |
| TRIGGERS | 触发 | EnvFactor → FunctionalSymptom | 环境因子诱发功能症状 |
| PROPAGATES | 传导 | Entity → Entity（通用） | 症状间、胁迫间、症状→产量传导 |
| ALLEVIATES | 缓解 | Treatment → Entity | 防治措施缓解症状/胁迫 |

子关系继承父关系 `causes` 的语义，图谱建边使用子关系而非 `causes` 本身，以支持路径查询时按因果语义类型筛选。判定逻辑在 [neo4j_client.py](file:///c:\Users\lenovo\Desktop\开题\bate\src\kg\neo4j_client.py) 的 `determine_edge_type` 函数中实现：

```python
def determine_edge_type(cause_class, effect_class, polarity="promote"):
    if cause_class == "EnvFactor" and effect_class == "FunctionalSymptom":
        return "TRIGGERS"
    if cause_class == "Treatment" and polarity == "inhibit":
        return "ALLEVIATES"
    return "PROPAGATES"
```

##### 2.3.2.4 节点多 label 存储方案

抽取出的实体节点采用多 label 方式存储到项目组本体的 Neo4j 实例，与本体 OWL 类层次完全对齐：

- **基础 label**：`:Entity`（所有实体节点的根 label）
- **大类 label**：`:EnvFactor` / `:BioticStress` / `:Treatment` / `:FunctionalSymptom`
- **子类 label**：`:WeatherParam` / `:SoilParam` / `:Disease` / `:YieldSymptom` 等

例如"高温胁迫"节点存储为：`(:Entity:EnvFactor:WeatherParam {concept: "高温胁迫", sub_class_name: "天气"})`。

这一多 label 方案直接对齐项目组本体 OWL 类层次结构 `owl:subClassOf`，保证：
1. 既可按大类快速查询所有环境因子，又可按子类精确筛选天气参数；
2. 因果边按 3 种子关系存储，路径查询使用 `[:TRIGGERS|PROPAGATES|ALLEVIATES]` 统一匹配；
3. 节点 `sub_class_name` 属性存储中文子类名（如"天气""土壤""水质"），前端图例展示时父类按红系着色，tooltip 显示中文子类名。

##### 2.3.2.5 模糊实体的本体层级存储规则

参照项目组本体文档 5.1 节，对抽取中无法精确归入子类的模糊实体，按以下规则在父类顶层存储：

- **性质明确的单一胁迫**（如"高温胁迫"）→ 归入相应子类（AirTemperatureParam）；
- **跨子类的复合胁迫**（如"干热风"涉及温度+湿度+风速）→ 存 WeatherParam 父类顶层，并通过 `composed_of` 关系描述其组成；
- **跨大类的复合胁迫**（如"旱风型"涉及 EnvFactor + BioticStress）→ 存 EnvFactor 顶层，避免在错误大类下创建新子类；
- **Treatment 缺失子类**（如化肥管理措施细分不足）→ 若 mention 属于未覆盖类型，`cause_event.concept` 输出 "UNKNOWN" 且 `sub_class_id` 留空，**不强行归到最近已知子类**，进入主动学习模块等待人工补全本体。

##### 2.3.2.6 存储到项目组本体的 Cypher 写入逻辑

抽取完成后，[neo4j_client.py](file:///c:\Users\lenovo\Desktop\开题\bate\src\kg\neo4j_client.py) 通过批量 UNWIND 参数化 + `execute_write` 事务写入 Neo4j。核心写入逻辑示意：

```cypher
// 节点写入（多 label）
UNWIND $nodes AS n
MERGE (e:Entity {concept: n.concept})
SET e.entity_class = n.entity_class,
    e.sub_class_id = n.sub_class_id,
    e.sub_class_name = n.sub_class_name
WITH e, n
CALL apoc.create.addLabels(e, [n.class_id, n.sub_class_id_only]) YIELD node
RETURN count(node)

// 因果边写入（3 种子关系）
UNWIND $edges AS e
MATCH (cause:Entity {concept: e.cause_concept})
MATCH (effect:Entity {concept: e.effect_concept})
CALL apoc.create.relationship(cause, e.edge_type, {
  confidence: e.confidence,
  relation_type: e.relation_type,
  evidence: e.evidence,
  evidence_source: e.evidence_source
}, effect) YIELD rel
RETURN count(rel)
```

写入流程使用 APOC 的 `create.addLabels` 和 `create.relationship` 实现动态 label 和动态关系类型，确保节点 label 层次和边类型完全对齐本体配置。

#### 2.3.3 实测图谱规模

在项目组本体 Neo4j 实例（密码：123456）上运行流水线后，实测图谱规模：

- **节点总数**：119
- **因果边总数**：239（TRIGGERS 111 + PROPAGATES 125 + ALLEVIATES 3）
- **禁止因果边**：71（FORBIDDEN_CAUSES，用于主动学习模块拒绝错误因果对）
- **实体大类分布**：FunctionalSymptom 91 / EnvFactor 20 / BioticStress 4 / Treatment 4
- **环境因子子类分布**：空气温度 8 / 天气（父类顶层）4 / 土壤湿度 4 / 光照 2 / 降水 1 / 土壤pH 1
- **功能症状子类分布**：生理症状 41 / 生殖症状 24 / 胁迫响应症状 12 / 产量症状 11 / 品质症状 3
- **生物胁迫子类分布**：虫害 2 / 病害 1 / 病原 1
- **防治措施**：4 个（化肥管理措施等，子类待补全）

样例因果链：
- 高温胁迫 → 穗粒数减少 → 产量下降（典型 EnvFactor TRIGGERS FunctionalSymptom PROPAGATES FunctionalSymptom 路径）
- 高温胁迫 → 植株徒长 → 养分分配失衡 → 胁迫缓解（多跳传导链）

### 2.4 没有这个方法会怎样

若不采用本文方法，常见的替代方案及其问题：

1. **多阶段串联 LLM 抽取**：1 条文本需 1+N+M 次 LLM 调用，成本高、上下文易断裂、多阶段输出不一致导致 Recall 坍塌（项目组早期实验已验证）。
2. **自由文本概念直接入库**：同一概念多种表述（"高温""热害""持续35℃以上高温"）会导致同义节点在图中爆炸式增殖，路径查询效率低且因果链断裂。
3. **无本体方向校验**：可能出现"Treatment→产量下降"这类语义反向错误，使下游因果推理出现"措施导致减产"的荒谬结论。
4. **无极性检查**：LLM 对"防效/病情指数"这类中性表达推理能力不足，常出现反向极性错误（如把"防效达 80%"识别为"病害加重"）。
5. **无本体对齐存储**：抽取的因果对停留在自由文本层，无法进入项目组 AKO-F v1.9 本体，形成知识孤岛，与项目组其他模块（知识图谱查询、问答、Agent）无法互操作。

### 2.5 方法时效性分析

本文方法在以下方面具备时效性：

- **LLM 调用最小化**：1 次联合抽取 + 规则后处理的设计，符合 2025 年学界"减少 LLM 调用、回归规则增强"的趋势（CausalRAG @Findings of ACL 2025 同样强调 LLM 在 RAG 中应作为最终生成器而非检索器）。
- **动态本体对齐**：从 [agri_event_types.json](file:///c:\Users\lenovo\Desktop\开题\bate\data\agri_event_types.json) 动态生成 LLM concept 选项，本体扩展时自动纳入新概念，避免硬编码 prompt 导致概念过期。
- **多子关系因果建模**：3 种子关系（TRIGGERS/PROPAGATES/ALLEVIATES）超越传统单一 CAUSES 关系，符合 2025 年细粒度因果建模趋势。
- **主动学习闭环**：低置信/未知子类因果对进入主动学习模块等待人工审核（[active_learning.py](file:///c:\Users\lenovo\Desktop\开题\bate\src\utils\active_learning.py)），符合 2025 年人机协同知识工程范式。

### 2.6 与主流论文对比

| 论文 | 发表会议/时间 | 中科院分区 | 核心方法 | 本文与之对比 |
| --- | --- | --- | --- | --- |
| **Event Causality Is Key** | NAACL 2024（2024.06，Mexico City） | 二区（CCF-B） | 强调事件因果识别在事件理解中的核心地位 | 本文同样以事件因果为抽取核心，但将事件层下沉为因果边，节点只承载实体概念，避免事件节点与实体节点混淆 |
| **LKCER** | COLING 2025（2025.01，Abu Dhabi） | 二区（CCF-B） | LLM 知识 + 概念级事件关系增强 | 本文借鉴其概念级聚合思想，但用同义词表 + 启发式规则替代 WordNet（中文农业覆盖差）和 COMET（农业领域噪声大），更适合中文农业文本 |
| **Paths to Causality** | KDD 2025（2025.08，Toronto） | 一区（CCF-A） | KG 子图 + LLM 因果发现 | 本文在抽取阶段即构建因果路径，而 Paths to Causality 是在已有 KG 上做因果发现，二者面向不同阶段，本文是上游、Paths 是下游 |
| **ShizishanGPT** | arXiv 2024.09（预印本，非同行评审） | 无分区 | 农业大模型 + AgriKG | 本文聚焦因果知识图谱，AgriKG 是通用知识图谱；二者互补，本文抽取的因果图可作为 ShizishanGPT 的因果增强模块 |
| **KnowQA** | Findings of EMNLP 2024（2024.11，Miami） | 二区（CCF-B 主会） | 知识引导二元问答 | 本文将因果路径作为知识引导，支持多跳问答，超越二元问答 |
| **SEAG** | Findings of ACL 2023（2023.07，Toronto） | 一区（CCF-A 主会） | text2text 因果图生成 | 本文用结构化抽取 + Neo4j 存储，SEAG 用 text2text 生成图描述；本文更适合图谱查询场景 |

对比总结：本文方法未过时，在"LLM 调用最小化""领域本体深度对齐""中文农业适用性""多子关系因果建模"四个维度上具备差异化优势。

---

## 3 研究内容2：面向农业认知任务的因果路径解释推理方法

### 3.1 研究目标

构建基于因果图的解释推理框架 CausalRAG（Causal Retrieval-Augmented Generation），实现：
- **问答目标**：将农业认知问题解析为因果路径查询；
- **检索目标**：在因果图上执行多跳路径检索，返回结构化因果链；
- **生成目标**：LLM 基于因果链生成可追溯的解释文本；
- **可解释目标**：每条答案都能回溯到图谱中的具体因果路径和证据。

### 3.2 要解决的问题

1. 传统 RAG 用 embedding 检索文本片段，缺乏因果结构，无法回答"为什么""如何传导""如何缓解"等因果问题；
2. 黑盒 LLM 直接生成答案，专家无法验证推理过程；
3. 多跳因果问题需要图算法支持，单纯文本检索难以胜任；
4. 农业专家需要"问题→证据→解释"完整链路，而非片段答案。

### 3.3 方法

CausalRAG 框架位于 [bate/src/kg/pipeline.py](file:///c:\Users\lenovo\Desktop\开题\bate\src\kg\pipeline.py)，4 步流程：

1. **问题解析**（[question_parser.py](file:///c:\Users\lenovo\Desktop\开题\bate\src\kg\question_parser.py)）：纯规则解析，0 次 LLM 调用，识别问题中的因果实体和问题类型（起因查询/传导查询/缓解查询）。
2. **因果路径检索**（[neo4j_client.py](file:///c:\Users\lenovo\Desktop\开题\bate\src\kg\neo4j_client.py)）：在 Neo4j 上执行 Cypher 路径查询，匹配 `[:TRIGGERS|PROPAGATES|ALLEVIATES]` 三种子关系，返回多跳因果链及置信度、证据来源。
3. **可解释生成**（[explanation_generator.py](file:///c:\Users\lenovo\Desktop\开题\bate\src\kg\explanation_generator.py)）：1 次 LLM 调用，将结构化因果链 + 证据原文组织为自然语言解释文本，标注因果路径节点和置信度。
4. **答案呈现**：解释文本 + 因果路径可视化 + 证据原文链接，支持前端点击节点跳转图谱。

### 3.4 没有这个方法会怎样

1. **传统向量 RAG**：检索片段与问题语义相似但无因果结构，"为什么高温导致减产"类问题难以回答，答案往往是文本片段堆砌。
2. **黑盒 LLM 直接问答**：LLM 内部推理不透明，专家无法验证，且 LLM 在垂直领域易产生幻觉，答案可信度低。
3. **纯图谱查询**：返回节点列表或路径，非农业专家难以理解，缺乏自然语言解释。
4. **多跳推理失效**：embedding 检索难以覆盖"高温→光合下降→养分失衡→产量下降"这类多跳传导，召回率低。

### 3.5 方法时效性分析

- **CausalRAG 范式**：本文方法直接对齐 2025 年 Findings of ACL 2025 论文 CausalRAG（Wang et al., 2025）提出的"因果图增强 RAG"范式，是该范式在中文农业领域的具体实现。
- **LLM 调用最小化**：仅生成阶段调用 1 次 LLM，符合 2025 年"LLM 作为最终生成器"的定位。

### 3.6 与主流论文对比

| 论文 | 发表会议/时间 | 中科院分区 | 核心方法 | 本文与之对比 |
| --- | --- | --- | --- | --- |
| **CausalRAG** | Findings of ACL 2025（2025.07，Vienna） | 一区（CCF-A 主会） | 因果图增强 RAG，将因果路径作为检索增强 | 本文是该范式在中文农业领域的落地，区别在于：(1) 因果图来自本研究内容1的抽取流水线，而非外部给定；(2) 路径查询使用 3 种子关系而非单一关系；(3) 问题解析纯规则、生成单次 LLM，成本更低 |
| **GraphRAG-Causal** | arXiv 2025.06（预印本，自称被 SIGIR 2025 接收） | 一区（CCF-A 主会 SIGIR） | Neo4j 属性图 + embedding 混合检索 | 本文路径检索为主、不依赖 embedding，更适合因果问答而非相似度问答；与 GraphRAG-Causal 互补，可后续融合 embedding 做语义补全 |
| **Paths to Causality** | KDD 2025（2025.08，Toronto） | 一区（CCF-A） | metapath 子图 + LTR 排序 | 本文路径检索基于 Cypher 直接查询，无需 metapath 抽取和排序模型，适合规模可控的农业因果图；在大规模图上可借鉴其 metapath 优化 |

对比总结：本文 CausalRAG 实现与 2025 年主流路径一致，在"垂直领域落地""3 子关系建模""LLM 成本最小化"上具备差异化价值，方法未过时。

---

## 4 结论与展望

### 4.1 结论

本文围绕"农业因果知识获取"与"因果路径解释推理"两个研究内容，提出并实现了一套端到端的方法体系：

1. **研究内容1**构建了"1 次 LLM 联合抽取 + 6 级规则概念聚合 + 本体方向校验 + 极性检查 + 3 维置信度评分 + DFS 因果链构建"的轻量化抽取流水线，重点解决了从项目组 AKO-F v1.9 本体到个人因果图的迁移对齐问题：实体层 4 大类→14 子类→概念三层映射、3 种因果子关系（TRIGGERS/PROPAGATES/ALLEVIATES）继承父关系 causes、多 label 节点存储、模糊实体按父类顶层存储、动态本体对齐等机制，保证抽取内容直接落库到项目组本体 Neo4j 实例。实测图谱规模 119 节点、239 因果边、71 禁止边。

2. **研究内容2**构建了 CausalRAG 因果路径解释推理框架，通过纯规则问题解析 + Cypher 路径查询 + 1 次 LLM 可解释生成，实现"问题→因果路径→解释文本"的可追溯认知任务支持。

3. **与 2024-2025 年主流论文对比**表明，本文方法在 LLM 调用最小化、领域本体深度对齐、中文农业适用性、多子关系因果建模四个维度具备差异化竞争力，方法路径未过时。

### 4.2 展望

未来工作包括：
1. 防治措施（Treatment）子类补全：当前 4 个 Treatment 节点子类待补全，通过主动学习模块积累人工审核数据扩充本体；
2. 多模态融合：将气象遥感数据、田间传感器数据接入因果图，支持实时环境因子触发推理；
3. embedding 与路径混合检索：借鉴 GraphRAG-Causal 思路，在因果路径检索基础上引入 embedding 做语义补全，提升召回率；
4. Agent 化：将 CausalRAG 包装为农业因果 Agent，支持多轮对话式因果追问与假设性推理。

---

## 参考文献

> 中科院分区（CAS Division）说明：中科院分区表（JCR期刊分区）主要针对 SCI 期刊，不直接覆盖计算机领域国际会议。对会议论文，学界惯例按 CCF 等级映射：CCF-A 类会议视同中科院一区，CCF-B 类视同中科院二区，CCF-C 类视同中科院三区。Findings 为相应主会的补充论文集，学术评价上略低于主会长文，但分区随主会。arXiv 预印本未经同行评审，无分区。

[1] Sun Y, Chao Q, Li B. Event Causality Is Key to Computational Story Understanding[C]. Proceedings of the 2024 Conference of the North American Chapter of the Association for Computational Linguistics: Human Language Technologies (NAACL 2024), 2024: 3493-3511, Mexico City, Mexico. **中科院二区**（CCF-B 类会议）.

[2] Su Y, Zhang H, Zhang G, et al. Enhancing Event Causality Identification with LLM Knowledge and Concept-Level Event Relations (LKCER)[C]. Proceedings of the 31st International Conference on Computational Linguistics (COLING 2025), 2025: 7403-7414, Abu Dhabi, UAE. **中科院二区**（CCF-B 类会议）.

[3] Wang Z, Xia L, Wang W, Du X. Document-level Causal Relation Extraction with Knowledge-guided Binary Question Answering (KnowQA)[C]. Findings of the Association for Computational Linguistics: EMNLP 2024, 2024: 16944-16955, Miami, FL, USA. **中科院二区**（CCF-B 类 EMNLP 主会）.

[4] Tao Z, Jin Z, Bai X, et al. SEAG: Structure-Aware Event Causality Generation[C]. Findings of the Association for Computational Linguistics: ACL 2023, 2023: 4631-4644, Toronto, Canada. **中科院一区**（CCF-A 类 ACL 主会）.

[5] Yang S, Liu Z, Mayer W, et al. ShizishanGPT: An Agricultural Large Language Model Integrating Tools and Resources[J]. arXiv preprint arXiv:2409.13537, 2024-09-20. **无分区**（arXiv 预印本，非同行评审）.

[6] Susanti Y, Färber M. Paths to Causality: Finding Informative Subgraphs Within Knowledge Graphs for Knowledge-Based Causal Discovery[C]. Proceedings of the 31st ACM SIGKDD Conference on Knowledge Discovery and Data Mining (KDD 2025), 2025-08-03/07, Toronto, ON, Canada. **中科院一区**（CCF-A 类会议）.

[7] Wang N, Han X, Singh J, et al. CausalRAG: Integrating Causal Graphs into Retrieval-Augmented Generation[C]. Findings of the Association for Computational Linguistics: ACL 2025, 2025: 22680-22693, Vienna, Austria. **中科院一区**（CCF-A 类 ACL 主会）. arXiv:2503.19878, 2025-03-25.

[8] Haque A, Hani U E, Din A, et al. GraphRAG-Causal: A Novel Graph-Augmented Framework for Causal Reasoning and Annotation in News[J]. arXiv preprint arXiv:2506.11600, 2025-06-13. **无分区**（arXiv 预印本；自称被 SIGIR 2025 接收，SIGIR 为 CCF-A 类会议，若属实视同中科院一区）.

[9] 农业知识图谱融合版本体构建报告 v1.9 (AKO-F v1.9). 项目组内部文档, 2026.
